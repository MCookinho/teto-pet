# Dependências Mate Helper para Windows
# Instalar com: pip install -r requirements-win.txt

# GTK / PyGObject no Windows requer MSYS2:
# 1. Instalar MSYS2: https://www.msys2.org/
# 2. No terminal MSYS2 UCRT64:
#    pacman -S mingw-w64-ucrt-x86_64-gtk3 mingw-w64-ucrt-x86_64-cairo \
#              mingw-w64-ucrt-x86_64-pango mingw-w64-ucrt-x86_64-python-gobject
# 3. Usar Python do MSYS2 UCRT64

# Core
requests>=2.28

# Screenshot
Pillow>=9.0

# TTS
edge-tts>=6.0
pyttsx3>=2.90

# STT (microfone)
pyaudio>=0.2.11
SpeechRecognition>=3.8

# Global hotkey
pynput>=1.7

# WebRTC VAD (opcional, para melhor detecção de voz)
# webrtcvad>=2.0

# AI
# Ollama: instalar separadamente de https://ollama.com/

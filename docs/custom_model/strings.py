# ── UI Strings ──────────────────────────────────────────────
# NOTE: Strings are now loaded from desktop_pet/strings.py
# (centralized, shared across all models).
#
# You can still override specific keys here if your model
# needs custom translations. Keys defined here take priority
# over the central strings.
#
# Structure:
#   STRINGS = {
#       "pt": { "key": "valor", ... },
#       "en": { "key": "value", ... },
#       "jp": { "key": "値", ... },
#   }
